# Fix: empty-state illustration + CSV export

- Adjusted mobile layout so the empty-state illustration is no longer clipped below 480px.
- Switched CSV export to a proper CSV-writer library that quotes/escapes commas and quotes in field values.
